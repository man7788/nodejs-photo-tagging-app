import './App.css';

function App() {
  return (
    <div
      className="App"
      // onClick={pop ? clickPicture : clickMenu}
      // style={{ cursor: cursor }}
      data-testid="App">
      <div className="title">{"Where're They?"}</div>
    </div>
  );
}

export default App;
